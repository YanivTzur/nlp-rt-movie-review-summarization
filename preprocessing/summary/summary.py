    #select all review with summary
    # summarsied_reviews = [movie for movie in  reviews_json['movies'] if movie['summary'] != 'No consensus yet.']
    # not_summarsied_reviews = [movie for movie in reviews_json['movies'] if movie['summary'] == 'No consensus yet.']
